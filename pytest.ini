[pytest]
pythonpath = src
addopts = -m "not manual"
testpaths = tests/smoke tests/unit/core tests/integration/core tests/integration/docs tests/integration/release
markers =
    manual: manually triggered smoke checks excluded from default test runs
